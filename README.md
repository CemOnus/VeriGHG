# GHG VB Prototype – with Sidebar Installer (warn-only)

This build detects missing `openpyxl` and offers a sidebar button to install it at runtime.

## Quick start
```bash
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate
pip install -r requirements.txt
streamlit run ghg_vb_app.py
```
