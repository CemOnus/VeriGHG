# Patched GHG VB – LOG mode (with openpyxl)

## Quick start
```bash
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate
pip install -r requirements.txt
streamlit run ghg_vb_app.py
```

This version adds `openpyxl` so Excel `.xlsx` files load correctly.
