# GHG Verification Body – Streamlit Prototype

## Quick start
```bash
python -m venv .venv
source .venv/bin/activate  # on Windows: .venv\Scripts\activate
pip install -r requirements.txt
streamlit run ghg_vb_app.py
```

### Demo inputs
Use any of the files under `demo_inputs/` inside the app:
- `sample_activity_data.xlsx`
- `supplier_data.docx`
- `travel_summary.pdf`
- `webpage_sample.html` (open locally and paste its file URL into the app's URL field)

> Emission factors and verification logic are illustrative for demo purposes only.
